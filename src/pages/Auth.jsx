import AuthContainer from "../Features/Authentication/AuthContainer";

export default function Auth() {
  return (
    <div className="AuthContainer w-full h-screen">
      {/*container xl:max-w-screen-xl */}
      <AuthContainer />
    </div>
  );
}
